# Phase 2-4 Implementation Plan

## Phase 2: SetFit Distillation Integration
1. Rewrite `incremental.py` for fusion architecture (currently broken - imports old tiers)
2. Add `DistillationManager` — monitors type library, triggers training at 50 samples/class
3. Auto-load trained model into E3 engine
4. Deployment validation — check F1 degradation before replacing model

## Phase 3: Type Discovery
5. Rewrite `discovery/loop.py` for new architecture (no StructuralClusterer/SemanticRefiner deps)
6. Connect E6 LLM is_new_type → discovery buffer
7. Type dedup — name similarity + semantic distance

## Phase 4: Verification
8. End-to-end integration test for fusion pipeline
9. Fault injection tests — degrade each engine, verify system survives
10. Latency benchmark — fusion_fast vs fusion_full timing
